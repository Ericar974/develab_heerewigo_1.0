const CallbackPage = () => {
    window.location.replace(window.location.origin);
    return (
        <p>Connexion established, you will soon be redirect...</p>
    );
};

export default CallbackPage;